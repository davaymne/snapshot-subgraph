type Delegation @entity {
  id: ID!
  delegator: String!
  space: String!
  delegate: String!
  timestamp: DateTime!
}

type Block @entity {
  id: ID!
  number: BigInt!
  timestamp: BigInt!
}

type Sig @entity {
  id: ID!
  account: Bytes!
  msgHash: String!
  timestamp: BigInt!
}
